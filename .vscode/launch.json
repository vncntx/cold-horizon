{
	"version": "0.1.0",
	"configurations": [
		{
			// Compile the extension and open it in a new window
			"name": "Launch Extension",
			"type": "extensionHost",
			"request": "launch",
			"runtimeExecutable": "${execPath}",
			"args": ["--extensionDevelopmentPath=${workspaceRoot}" ]
		}
	]
}
